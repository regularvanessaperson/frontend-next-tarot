const Today = () => {
    return <h1>This will be the today page that renders when a user logs in it should display the text input box and a button to pull the reading of the day</h1>
}

export default Today